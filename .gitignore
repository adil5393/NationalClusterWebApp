# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# IDE and editors
.idea/
.vscode/
/*.xlsx
/excel_exports

# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Production builds
/build
dist/
dist

# Environment files (comprehensive coverage)

*token.json*
*credentials.json*

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb

# System files
.DS_Store
*.pem

# Python
__pycache__/
*pyc*
venv/
.venv/

# Development tools
chainlit.md
.chainlit
.ipynb_checkpoints/
.ac

# Deployment
.vercel

# Data and databases
agenthub/agents/youtube/db
# Postgres runs off a named Docker volume (see docker-compose.yml), never this
# path — if it reappears here it's a stray/orphaned copy, not live data.
/data/postgres/

# Archive files and large assets
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb
*.dylib

# Build caches
.cache/

memory/test_credentials.md

# Mobile development
android-sdk/
.env
.env.*
*.env
credentials.json
*.key
.credentials
.claude/

/backend/tests/*

*test*.py
backend/make_school_images.py
# Runtime-uploaded content (gallery/participant/team photos) lives under
# here and is never committed — but id_card_template.png and the receipt's
# school-logo monogram are versioned design assets the app needs to exist on
# every checkout, so they're carved out.
backend/assets/*
!backend/assets/templates/
!backend/assets/monogram/

frontend/public/images/

# Raw camera originals (8-16MB each) — see frontend/scripts/optimize-school-photos.py.
# The resized WebP output it produces (images-optimized/) is what the app
# actually uses and stays tracked.
frontend/src/assets/images/